import numpy as np

array = np.array([1, 2, 3, 4, 5])

# Operaciones matematicas
array_mul = array * 2      # Multiplicación por 2
array_sum = array + 3      # Suma de 3
array_sub = array - 1      # Resta de 1
array_div = array / 2      # División entre 2
array_pow = array ** 2     # Potencia al cuadrado
array_mod = array % 2      # Módulo 2

# zero

zeros_one = np.zeros((3,3))
zero_two = np.ones((3,3))
suma_array =  np.sum(zero_two)
cantidad = zeros_one.dtype

print(cantidad)